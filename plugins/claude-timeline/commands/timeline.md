---
description: Start the claude-timeline live dashboard and open it in the browser
---

Run `bash ${CLAUDE_PLUGIN_ROOT}/scripts/start.sh` and report the URL.

If the user passes `--no-open`, forward that flag so it starts the server without opening a browser tab.
